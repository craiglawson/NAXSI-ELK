filter {
  if [type] == "nginx-error" {
    grok {
        match => [ "message", "%{NGINXNAXSI_EXLOG}" ]
    }
  }

  if [type] == "nginx-access" {
    grok {
        match => { "message" => "%{NGINXACCESS}" }
    }
  }
}